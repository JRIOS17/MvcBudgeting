@model IEnumerable<MvcBudgeting.Models.ExpenseSheet>

@{
    ViewData["Title"] = "Index";
}

<h1>List all of your Expenses here!</h1>
<br /><br />
@*Before the form there will be an if/else statement asking the user to setup a username*@
<div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.Label("Expense Date")
                </th>
                <th>
                    @Html.Label("Expense Category")
                </th>
                <th>
                    @Html.Label("Expense Sub-Category")
                </th>
                <th>
                    @Html.Label("Expense Amount")
                </th>
                <th>
                    @Html.Label("Expense Item")
                </th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
        <div id="expensesPartial">
            @* @foreach(ExpenseSheet expense in Model)
            {
                <div id="ExpenseHeaderClass"></div>
            } *@
            <div id="expenseList">

            </div>

            <partial name="_Test.cshtml" />

            <div>
                <button class="button" id="btn_addExpense">Add a new Expense</button>
            </div>
        </div>
</div>

@section Scripts {
    <script>
        //Test to load the Testing partial view that worked
        // $('#btn_addExpense').on('click', function(e){
        //     $.ajax({
        //         url: '/ExpenseSheets/TestPartial',
        //         type: 'GET',
        //         dataType: "html"
        //     }).done(function(result){
        //         $('#expenseList').html(result);
        //     }).fail(function(xhr){
        //         console.log('error: ' + xhr.status);
        //     });
        // });

        //1st attempt
        // $(document).on('click', '#btn_addExpense', function (e){
        //     $.ajax({
        //         url: '/ExpenseSheets/DisplayNewExpense',
        //         success: function(partialView){
        //             $('#ExpenseHeaderClass').append(partialView);
        //         }
        //     })
        // })

        //METHOD THAT WORKS!!! FOR NOW....
        $('#btn_addExpense').on('click', function(e){
            $.ajax({
                url: '/ExpenseSheets/DisplayNewExpense',
                type: 'GET',
                dataType: "html"
            }).done(function(result){
                $('#expenseList').append(result);
            }).fail(function(xhr){
                console.log('error: ' + xhr.status);
            });
        });

        //2nd attempt
        // $('#btn_addExpense').click(function() {
        //     $('#expenseList').append('/ExpenseSheets/TestPartial');
        // })

        //3rd attempt
        // var url = '@Url.Action("TestPartial", "ExpenseSheets")'
        // $('#btn_addExpense').on('click', function() {
        //     $('#expenseList').load(url)
        // });

        //4th attempt
        // $('#btn_addExpense').click(function() {
        //     $('#expenseList').load('/ExpenseSheets/TestPartial');
        // })

        //5th attempt
        // const button = document.getElementById("btn_addExpense");
        // button.onclick = function(){
        //     $('#expenseList').load(url)
        // };

    </script>
}

@* <p>
    <a asp-action="Create">Create New Expense Record</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenseCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenseSubCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenseAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenseItem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseCategory)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseSubCategory)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseItem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@